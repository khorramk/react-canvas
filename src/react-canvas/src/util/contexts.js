import React from 'react';
let Context = {
    SceneContext : React.createContext(),
    BoxContext: React.createContext(),
    CameraContext: React.createContext(),
    Rendercontext: React.createContext()
}

export default Context;